#!/bin/bash

# Set the source directory
src="/path/to/src/"

# Set the destination directory and filename
dst="/path/to/dst/backup_$(date +%F).zip"

# Create a zip archive of the source directory and store it in the destination
zip -r "$dst" "$src"
