#!/bin/bash

# Set the source directory
src="/home/backup/"

# Set the destination for the zip file
dst="/home/backup_$(date +%F).zip"

# Create a zip archive of the source directory and store it in the destination
zip -r "$dst" "$src"
